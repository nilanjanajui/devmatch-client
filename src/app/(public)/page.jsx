export default function HomePage() {
    return (
        <main className="flex items-center justify-center min-h-screen">
            <h1 className="text-4xl font-bold text-gradient font-heading">
                DevMatch is coming.
            </h1>
        </main>
    );
}